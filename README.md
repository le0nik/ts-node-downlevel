# Reproduction of a bug with combination of lib.es2015.iterable + downlevelIteration

Try running `npm test` first with both `es2015.iterable` and `downlevelIteration` then without at least 1 of them in `tsconfig.json`

